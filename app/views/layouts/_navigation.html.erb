<%# if NOT signed in%>
  <%= link_to "Sign up", new_user_path %>
  <%= link_to "Sign Up", new_user_path %>
  <%= link_to "Sign in", signin_path %>
<%# end %>

<% if is_logged_in?(session) %>
  <%= link_to "Log Out", session_path(User.find_by(id: session[:user_id])), method: "delete" %>
<% end %><br><br>

<%= link_to "See attractions", attractions_path %>